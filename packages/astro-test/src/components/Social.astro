---
interface Props {
  platform?: string;
  username?: string;
}

const { platform, username } = Astro.props;
---

<a class="cta" href={`https://www.${platform}.com/${username}`} target="_blank">
  {platform}
</a>
